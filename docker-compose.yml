services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      API_KEY: ${API_KEY:-dev-secret}
      DB_PATH: "/data/dogs.db"
      VITE_API_KEY: ${VITE_API_KEY:-dev-secret}
    volumes:
      - db_volume:/data

volumes:
  db_volume:
